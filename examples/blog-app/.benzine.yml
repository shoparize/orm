benzine:
  application:
    name: BlogApp
  logging:
    format_message: "%level_name% > %message%"
  migrations:
    - ./blog.sql
  databases:
    mysql:
      type: mysql
      host: mysql
      port: 3306
      username: root
      password: ChangeMe
      database: blog